// @nonchalant/dom — tag constructors and the DOM sink. Lands at M4.
// Nodes are plain typed data ({ tag, attrs, children }); the sink builds them with
// createElement/createTextNode/setAttribute — no string is ever parsed as markup.
// Subpaths planned: ./tags (named imports, var_ escapes), ./jsx (~50-line adapter).
export {}
